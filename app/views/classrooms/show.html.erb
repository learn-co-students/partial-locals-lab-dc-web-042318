Classroom Info
<%= render partial: 'classroom', locals: {classroom: @classroom} %>

We now want to call out the oldest student in the class:
<%= @classroom.students.each do |stud| %>
<p><%= render partial: 'students/student', locals: {student: stud}  %><p>
<% end %>